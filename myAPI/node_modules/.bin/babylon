#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../_babylon@6.18.0@babylon/bin/babylon.js" "$@"
else 
  exec node  "$basedir/../_babylon@6.18.0@babylon/bin/babylon.js" "$@"
fi
